#include "main.h"
/**
 * main -entry point
 * print the alphabet in lowercase
 * Return: Always 0 (success)
 */
void print_alphabet(void)
{
int i;
for (i = 97; i <= 122; i++){
_putchar(i);
}
_putchar(i);
return (0);
}
