#ifndef SUM_H
#define SUM_H

/**
 * File: 4-sum.h
 * Description: function-like macro SUM(x, y)
 *              that computes the sum of the numbers x and y.
 * @x: first number 
 * @y: second number added with first number
 */

#define SUM(x, y) ((x) + (y))

#endif
